// @flow
// src/types/trsWordTable.js

/**
 * @typedef {[string, string, string]} WordEntry
 * Represents a single entry in the word table: [original_word, translated_word, category]
 */

/**
 * @typedef {WordEntry[]} TrsWordTable
 * Represents the structure of word_table.json
 */

// TypeScript equivalent:
// export type WordEntry = [string, string, string];
// export type TrsWordTable = WordEntry[];
